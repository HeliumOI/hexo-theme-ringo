<% if (is_page() && page.type === 'about') { %>
	<%- partial('_partial/article', {post: page, index: false}) %>
<% } %>
<% if (is_page() && page.type === 'friends') { %>
	<%- partial('_partial/page/friends') %>
<% } %>
<% if (is_page() && page.type === 'categories') { %>
	<%- partial('_partial/page/categories', {list: site.categories, type: 'categories'}) %>
<% } %>
<% if (is_page() && page.type === 'tags') { %>
	<%- partial('_partial/page/tags') %>
<% } %>